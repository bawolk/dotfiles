return {
  "saghen/blink.cmp",
  opts = {
    -- Avoid use of <enter> key
    keymap = { preset = "default" },
    -- Not using mono Nerd font
    appearance = { nerd_font_variant = "normal" },
  },
}
